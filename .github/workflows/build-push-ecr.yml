name: Build, Scan, Patch, and Push to ECR

on:
  push:
    branches:
      - master

jobs:
  build-scan-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Bandit for Python security scanning
        run: |
          pip install bandit

      - name: Run Bandit security scan
        run: |
          bandit -r . -f json -o bandit-results.json
        continue-on-error: true

      - name: Upload Bandit scan results
        uses: actions/upload-artifact@v4
        with:
          name: bandit-results
          path: bandit-results.json

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Log in to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1
        with:
          mask-password: true

      - name: Build Docker image
        run: |
          docker build -t my-python-app:${{ github.sha }} .


      - name: Scan Docker image with Trivy
        id: trivy-scan
        run: |
          # Create the trivy-results directory
          mkdir -p trivy-results

          # Run Trivy and save the output to the trivy-results directory
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            -v $(pwd)/trivy-results:/trivy-results \  # Mount the trivy-results directory
            aquasec/trivy:latest \
            image --severity HIGH,CRITICAL --exit-code 1 --format json --output /trivy-results/trivy-results.json my-python-app:${{ github.sha }}

          # Check if the file was created
          if [ -f "trivy-results/trivy-results.json" ]; then
            echo "Trivy scan results saved to trivy-results/trivy-results.json"
          else
            echo "Trivy scan results file not found!"
            exit 1
          fi
        continue-on-error: true

      - name: Debug working directory
        run: |
          ls -la

      - name: Upload Trivy scan results
        uses: actions/upload-artifact@v4
        with:
          name: trivy-results
          path: trivy-results/trivy-results.json

      - name: Check Trivy scan results
        if: steps.trivy-scan.outcome == 'failure'
        run: |
          echo "Trivy found vulnerabilities in the Docker image."
          echo "Proceeding with the next steps, but please review the Trivy scan report."

      - name: Tag Docker image
        run: |
          docker tag my-python-app:${{ github.sha }} ${{ steps.login-ecr.outputs.registry }}/my-python-app:${{ github.sha }}

      - name: Push Docker image to ECR
        run: |
          docker push ${{ steps.login-ecr.outputs.registry }}/my-python-app:${{ github.sha }}
